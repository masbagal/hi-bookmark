{
  "name": "hi-bookmark-api",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --target=bun --outfile=dist/index.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "drizzle-orm": "^0.37.0",
    "hono": "^4.6.12",
    "mysql2": "^3.11.5",
    "schema": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.29.1"
  }
}
